---
description: Write zero-ambiguity implementation plans with actual code in every task. Mandatory fields per task → Goal, File (exact path), Code (complete, no placeholders), Verify command, Checkpoint. Use before /execute-plan.
argument-hint: "[feature or task description] [@path/to/project]"
allowed-tools: none
destructive: true
read-only: false
---

Invoke the write-plan skill:

/one-shot-prompting:write-plan $ARGUMENTS
