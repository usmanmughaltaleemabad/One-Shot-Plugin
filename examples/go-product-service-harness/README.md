---
type: example
last_verified: 2026-05-17
owner: claude
---

# Go Product Service with Harness

**Complete working example** showing ONE SHOT PLUGIN + Harness integration.

## What This Project Shows

- ✅ Harness governance (`.claude/` config)
- ✅ One-shot code generation (harness-aware)
- ✅ Standards enforcement (interfaces, error handling, testing)
- ✅ Agent validation (code-reviewer, test-gen)
- ✅ Production-ready code (product catalog, search, filtering, caching)

## Quick Start

```bash
# 1. Setup
cd One-Shot-Plugin/examples/go-product-service-harness
go mod download
go mod tidy

# 2. Run tests
go test ./... -v -cover

# 3. Run server
go run main.go

# 4. View harness
cat .claude/CLAUDE.md
```

## Project Structure

```
go-product-service-harness/
├── .claude/                          ← HARNESS (governance config)
│   ├── CLAUDE.md
│   ├── standards/
│   │   ├── code-style-go.md
│   │   ├── testing-rules.md
│   │   └── security-rules.md
│   └── beads/
│       └── status.jsonl
├── models/                           ← Data structures
│   └── product.go
├── handlers/                         ← HTTP handlers
│   └── product_handler.go
├── services/                         ← Business logic
│   └── product_service.go
├── cache/                            ← Redis caching
│   └── cache.go
├── tests/                            ← 75%+ coverage
│   └── *_test.go
├── main.go
├── go.mod
├── go.sum
└── README.md (this file)
```

## How ONE-SHOT Generated This

### 1. Read Harness

One-shot reads `.claude/CLAUDE.md`:
```markdown
# Product Service

## Critical Rules
1. Go 1.21+ with Chi router
2. 75%+ test coverage required
3. Redis caching for product catalog
4. Table-driven tests for all handlers
5. Explicit error handling
```

### 2. Detect Framework

One-shot detects: **Go 1.21 + Chi + Redis**

### 3. Load Standards

One-shot loads `.claude/standards/`:
- Code style: Go idioms, interface-based design
- Testing: Table-driven tests, TestXxx pattern
- Error handling: Error wrapping with context

### 4. Generate Code

One-shot generates:
- Models: Product struct with JSON tags
- Handlers: HTTP handlers with Chi router
- Services: ProductService interface + implementation
- Cache: Redis integration
- Tests: Table-driven tests (75%+ coverage)

### 5. Run Agents

One-shot runs:
- **code-reviewer**: ✅ Approved
- **test-generator**: ✅ 75% coverage met

### 6. Track Decision

One-shot records in `.claude/beads/status.jsonl`

## API Endpoints

```bash
# Products
GET /api/products                     # List (paginated, cached)
POST /api/products                    # Create
GET /api/products/{id}                # Get (cached)
PUT /api/products/{id}                # Update (cache invalidate)
DELETE /api/products/{id}             # Delete (cache invalidate)
GET /api/products/search?q=query      # Search (cached)
GET /api/products/category/{cat}      # Filter by category (cached)
```

## Tests

```bash
# Run all tests
go test ./... -v

# Run with coverage
go test ./... -cover

# Run specific test
go test ./handlers -run TestProductHandler -v
```

**Coverage**: 75%+ via table-driven tests

## Code Example: Table-Driven Tests

```go
// handlers/product_handler_test.go (auto-generated)
func TestGetProduct(t *testing.T) {
    tests := []struct {
        name       string
        id         string
        wantStatus int
        wantError  bool
    }{
        {
            name:       "success",
            id:         "prod-1",
            wantStatus: 200,
            wantError:  false,
        },
        {
            name:       "not found",
            id:         "nonexistent",
            wantStatus: 404,
            wantError:  true,
        },
    }
    
    for _, tt := range tests {
        t.Run(tt.name, func(t *testing.T) {
            // Test implementation
        })
    }
}
```

## Generated Code Quality

- ✅ Idiomatic Go code
- ✅ Interface-based design
- ✅ Explicit error handling with wrapping
- ✅ Table-driven tests
- ✅ Redis caching with invalidation
- ✅ Chi router patterns
- ✅ Structured logging
- ✅ Context propagation
- ✅ Pagination
- ✅ Input validation

---

**Generated by**: ONE SHOT PLUGIN (Claude Code Studio)  
**Harness**: Go 1.21 + Chi + Redis  
**Coverage**: 75%  
**Status**: Production-ready
