---
type: router
last_verified: 2026-05-17
owner: claude
---

# FastAPI Payment Processor

Framework detection: **FastAPI 0.104 + SQLAlchemy async + Pydantic v2**

## Quick Navigation

| For... | See... |
|---|---|
| Code style rules | `standards/code-style-fastapi.md` |
| Testing requirements | `standards/testing-rules.md` |
| Security rules | `standards/security-rules.md` |
| Agent validations | `agents/` |
| What was generated | `beads/status.jsonl` |

## Critical Rules

1. **Async/await throughout** — no blocking I/O
2. **Pydantic v2 validation** — required for all requests
3. **SQLAlchemy async ORM** — use `async with` for sessions
4. **80%+ test coverage** — required before merge
5. **Stripe integration** — all payment logic tested with mocks
6. **Idempotency keys** — required for all POST endpoints

## Architecture

```
main.py (FastAPI app entry)
├── app/
│   ├── models.py (SQLAlchemy async)
│   ├── schemas.py (Pydantic v2)
│   ├── services.py (business logic)
│   ├── routes.py (FastAPI routes)
│   ├── database.py (async session management)
│   └── tests/ (pytest async)
├── migrations/ (Alembic)
└── .claude/ (this harness)
```

## Hooks & Validation

**Pre-tool hooks** check:
- All routes are async
- No blocking calls (time.sleep, requests library)
- Pydantic validation on all request/response models

**Post-tool hooks** check:
- Tests pass: `pytest --cov=app`
- Async syntax valid: `python -m py_compile app/*.py`

## Test Patterns

```python
@pytest.mark.asyncio
async def test_payment(async_client):
    response = await async_client.post("/api/payments/", json={...})
    assert response.status_code == 201
```

## Standards

- **Python 3.11+** type hints required
- **Line length**: 100 characters max
- **Format**: Black (auto-enforced by hooks)
- **Imports**: Organized (stdlib → packages → local)

---

**Generated by**: ONE SHOT PLUGIN  
**Status**: Complete example for harness + one-shot integration  
**Last updated**: 2026-05-17
