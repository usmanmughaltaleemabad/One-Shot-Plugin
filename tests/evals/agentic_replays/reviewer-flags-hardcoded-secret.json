{
  "scenario": "reviewer-flags-hardcoded-secret",
  "agent": "reviewer",
  "fixture_type": "contract-test",
  "note": "Reviewer must flag hardcoded secrets with REVISE verdict + file:line.",
  "input": {
    "review_target": "config.py",
    "code_under_review": "API_KEY = \"sk_live_4242abc123XYZ\"\nDB_PASSWORD = \"hunter2\"\n\ndef connect():\n    return db.connect(password=DB_PASSWORD)"
  },
  "expected_output_contract": {
    "expected_verdict": "REVISE"
  },
  "recorded_output": "REVIEW: REVISE\n\nconfig.py:1 — hardcoded API_KEY committed to source.\nconfig.py:2 — hardcoded DB_PASSWORD committed to source.\n\nBoth values will live in git history permanently even if rotated. The API_KEY string pattern (sk_live_*) looks like a live Stripe key.\n\nFix at config.py:1-2 — load from environment:\n    from pydantic_settings import BaseSettings\n    class Settings(BaseSettings):\n        api_key: str\n        db_password: str\n        class Config: env_file = '.env'\n\nAdd .env to .gitignore. If these values were committed, rotate them now.\n\nSeverity: critical.\n"
}
